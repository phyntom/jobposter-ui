import { Button } from "./ui/button";

const ViewAll = () => {
  return (
    <div className="m-auto flex justify-center w-full">
      <Button className="bg-primary">View All Jobs</Button>
    </div>
  );
};

export default ViewAll;
